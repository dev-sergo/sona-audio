[pytest]
asyncio_mode = auto
testpaths = tests
# Put the repo root on sys.path so `import server` / `bot` / `model_server` works
# regardless of how pytest is launched (bare `pytest` in CI vs `python -m pytest`).
pythonpath = .
